# Sirikul Accounting & Tax — Client Portal & Admin Dashboard

ระบบบริหารสำนักงานบัญชีแบบ **ไฟล์เดียว** (`index.html`) เก็บข้อมูลด้วย `localStorage`
ใช้งานได้ทันทีโดยเปิดไฟล์ `index.html` ในเบราว์เซอร์ — ไม่ต้องติดตั้งอะไรเพิ่ม

> ระบบนี้พัฒนาต่อจากแบบดีไซน์ที่ให้มา (7 หน้าจอ) และเปลี่ยนแบรนด์เป็นของเราเอง

---

## 🔑 การเข้าสู่ระบบ (ทดลองใช้)

หน้าล็อกอินให้เลือก **ประเภทผู้ใช้งาน** แล้วกรอกอีเมล/รหัสผ่าน

| บทบาท | ประเภทผู้ใช้งาน | Username | รหัสผ่าน |
|-------|----------------|----------|----------|
| เจ้าของระบบ / ผู้ดูแล | เจ้าของระบบ | `admin` | `admin` |
| ลูกค้า (บ. สยามเทรดดิ้ง) | ลูกค้า | `somchai@siamtrading.co.th` | `1234` |

ลูกค้ารายอื่นใช้อีเมลตามข้อมูลในระบบ (รหัสผ่าน `1234` ทุกราย) เช่น
`malee@bansuan.com`, `weera@digitalplus.co.th`, `prayut@rungruang.com`,
`napha@greenfarm.co.th`, `siriporn@radiance.co.th`

> เว้นว่างช่องอีเมล/รหัสผ่านก็เข้าได้ (โหมดสาธิต) — ผู้ดูแลจะเข้าเป็น admin, ลูกค้าจะเข้าเป็นสยามเทรดดิ้ง

---

## 👤 บทบาทและเมนู

### เจ้าของระบบ / ผู้ดูแล (Admin Dashboard)
- **ภาพรวม** — แดชบอร์ด (Data Analytics), ปฏิทินงานและภาษี, แจ้งเตือน
- **ลูกค้าและเอกสาร** — ลูกค้า, เอกสารลูกค้า, ตรวจสอบเอกสาร, แจ้งเอกสารขาด, การอนุมัติงานลูกค้า, ประวัติเอกสารลูกค้า
- **งานสำนักงาน** — งานทั้งหมด, งานของฉัน, งานที่มอบหมายให้ทีม, คลังไฟล์สำนักงาน, งานที่แชร์ให้ลูกค้า, ประวัติการแชร์
- **งานบัญชีและภาษี** — งานบัญชีรายเดือน, งานภาษี, งานปิดงบ
- **การเงินในงานบัญชี** — ลูกหนี้ค่าบริการ, ใบแจ้งหนี้, รายงานรายได้
- **ตั้งค่าระบบ** — ผู้ใช้งาน, บทบาทและสิทธิ์, Activity Log, ตั้งค่าระบบ

### ลูกค้า (Client Portal)
- **ภาพรวม** — แดชบอร์ด, ปฏิทินงาน/กำหนดส่ง, สถานะงานบัญชี, แจ้งเตือน
- **เอกสารของฉัน** — อัปโหลดเอกสาร, เอกสารของฉัน, เอกสารที่ยังขาด, เอกสารที่ส่งแล้ว, ประวัติการส่ง/แก้ไข/ลบ
- **งานจากสำนักงาน** — งานที่สำนักงานแชร์ให้, เอกสารจากสำนักงาน, รายงานจากสำนักงาน
- **การยืนยันและการแก้ไข** — ยืนยันข้อมูล, คำขอแก้ไขเอกสาร, เอกสารที่ต้องแก้ไข
- **ข้อมูลบริษัท** — ข้อมูลบริษัท, ผู้ใช้งานในบริษัท, ตั้งค่าบัญชีผู้ใช้

---

## ✏️ เปลี่ยนชื่อ/แบรนด์

แก้ที่ค่าคงที่ `BRAND` ด้านบนของ `<script>` ใน `index.html` ที่เดียว:

```js
const BRAND = {
  full: 'Sirikul Accounting & Tax',   // ชื่อเต็ม
  short: 'Sirikul Acc.',              // ชื่อย่อ (แถบข้าง)
  logoName: '·SIRIKUL·',              // ชื่อบนโลโก้
  logoSub: 'ACCOUNTING & TAX',        // คำบรรยายใต้โลโก้
};
```

## ♻️ รีเซ็ตข้อมูล
เมนู **ตั้งค่าระบบ → รีเซ็ตข้อมูลตัวอย่าง** (ฝั่งเจ้าของระบบ) หรือลบ key `sirikul_app_v1` ใน localStorage

## 🗓️ หมายเหตุ
วันอ้างอิงของระบบตั้งไว้ที่ **12 มิ.ย. 2569** (ค่าคงที่ `NOW`) เพื่อให้ตัวเลข/สถานะตรงกับแบบที่ออกแบบไว้
